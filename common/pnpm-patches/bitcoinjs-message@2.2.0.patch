diff --git a/CHANGELOG.md b/CHANGELOG.md
deleted file mode 100644
index 00a5ffdc397d9e1d25ccef95876536b27d96850a..0000000000000000000000000000000000000000
diff --git a/index.js b/index.js
index beaf57ec2b6f836898d1b55b15aba661a6cd2386..91a872b9905178420ccaa595bf6c2407c671f9d4 100644
--- a/index.js
+++ b/index.js
@@ -2,7 +2,7 @@ const bs58check = require('bs58check')
 const bech32 = require('bech32')
 const bufferEquals = require('buffer-equals')
 const createHash = require('create-hash')
-const secp256k1 = require('secp256k1')
+const secp256k1 = require('@bitcoinerlab/secp256k1')
 const varuint = require('varuint-bitcoin')
 
 const SEGWIT_TYPES = {
@@ -94,10 +94,10 @@ function prepareSign (
   ) {
     throw new Error(
       'Unrecognized segwitType: use "' +
-        SEGWIT_TYPES.P2SH_P2WPKH +
-        '" or "' +
-        SEGWIT_TYPES.P2WPKH +
-        '"'
+      SEGWIT_TYPES.P2SH_P2WPKH +
+      '" or "' +
+      SEGWIT_TYPES.P2WPKH +
+      '"'
     )
   }
 
@@ -112,7 +112,7 @@ function isSigner (obj) {
   return obj && typeof obj.sign === 'function'
 }
 
-function sign (
+function _sign (
   message,
   privateKey,
   compressed,
@@ -124,18 +124,57 @@ function sign (
     segwitType,
     extraEntropy
   } = prepareSign(messagePrefix, sigOptions)
+
   const hash = magicHash(message, messagePrefixArg)
-  const sigObj = isSigner(privateKey)
-    ? privateKey.sign(hash, extraEntropy)
-    : secp256k1.sign(hash, privateKey, { data: extraEntropy })
+
+  let signature
+  let recovery
+
+  if (isSigner(privateKey)) {
+    const response = privateKey.sign(hash, extraEntropy)
+
+    signature = response.signature
+    recovery = response.recovery
+  } else {
+    signature = secp256k1.sign(hash, privateKey, extraEntropy)
+
+    const publicKey = secp256k1.pointFromScalar(privateKey, compressed)
+
+    for (let index = 0; index < 4; index++) {
+      let recovered
+
+      try {
+        recovered = secp256k1.recover(hash, signature, index, compressed)
+      } catch {}
+
+      if (recovered && Buffer.from(recovered).equals(Buffer.from(publicKey))) {
+        recovery = index
+      }
+    }
+
+    if (typeof recovery !== 'number') {
+      throw new Error('Could not determine the recovery')
+    }
+  }
+
   return encodeSignature(
-    sigObj.signature,
-    sigObj.recovery,
+    signature,
+    recovery,
     compressed,
     segwitType
   )
 }
 
+function sign (
+  message,
+  privateKey,
+  compressed,
+  messagePrefix,
+  sigOptions
+) {
+  return _sign(message, privateKey, compressed, messagePrefix, sigOptions)
+}
+
 function signAsync (
   message,
   privateKey,
@@ -143,24 +182,10 @@ function signAsync (
   messagePrefix,
   sigOptions
 ) {
-  let messagePrefixArg, segwitType, extraEntropy
   return Promise.resolve().then(() => {
-    ({
-      messagePrefixArg,
-      segwitType,
-      extraEntropy
-    } = prepareSign(messagePrefix, sigOptions))
-    const hash = magicHash(message, messagePrefixArg)
-    return isSigner(privateKey)
-      ? privateKey.sign(hash, extraEntropy)
-      : secp256k1.sign(hash, privateKey, { data: extraEntropy })
-  }).then((sigObj) => {
-    return encodeSignature(
-      sigObj.signature,
-      sigObj.recovery,
-      compressed,
-      segwitType
-    )
+    return _sign(message, privateKey, compressed, messagePrefix, sigOptions)
+  }).then(signature => {
+    return signature
   })
 }
 
diff --git a/package.json b/package.json
index bc1e4e92ef5599d96514caa563db587f46264a20..79d6607bcc2a0b74164d96889b7fff78366ac4cc 100644
--- a/package.json
+++ b/package.json
@@ -29,11 +29,11 @@
     "unit": "tape test/*.js"
   },
   "dependencies": {
+    "@bitcoinerlab/secp256k1": "~1.2.0",
     "bech32": "^1.1.3",
     "bs58check": "^2.1.2",
     "buffer-equals": "^1.0.3",
     "create-hash": "^1.1.2",
-    "secp256k1": "^3.0.1",
     "varuint-bitcoin": "^1.0.1"
   },
   "devDependencies": {
